# 风擎工控 AeroForge MES 后端接口清单（前端开发版）

> 生成日期：2026-07-11  
> 依据：当前后端 Controller、网关路由和动态资源白名单。本文记录的是**当前代码已实现接口**，不把前端 TODO 路径当作已实现接口。

## 1. 全局调用规范

- 前端统一访问网关：开发环境默认 `http://localhost:8080/api`。前端请求封装若已配置 `/api` 基地址，业务方法只写如 `/production/work-orders`。
- 除登录和健康检查外，生产启用鉴权后统一携带 `Authorization: Bearer <token>`。当前网关配置 `auth-enabled: false` 仅适合本地联调。
- JSON 请求使用 `Content-Type: application/json`；SOP 附件和 3D 模型上传使用 `multipart/form-data`，不要手工固定 boundary。
- 两套公共响应包装并存：`Result<T>` 与 `ApiResponse<T>`。前端必须通过统一拦截器解包，不要在页面中分别判断。典型结构为 `{ code, message, data }`；是否成功以当前公共类定义的成功码为准。
- 列表型动态资源返回的数据通常包含记录集合及分页信息。查询参数以字符串传输；日期时间建议使用 ISO-8601 格式，数量使用数字，主键使用整数。
- 状态流转必须调用 `/issue`、`/start`、`/complete`、`/close`、`/void` 等动作接口，禁止通过普通 PUT 直接篡改状态。
- 所有用户可见错误优先展示后端 `message`；401 清理登录态，403 跳转无权限页，400 展示参数或状态错误，5xx 提供重试入口。

### 1.1 网关服务映射

| 微服务 | 直连端口 | 网关前缀 | 职责 |
|---|---:|---|---|
| 网关服务（mes-gateway） | 8080 | `/api/gateway` | 统一入口、鉴权过滤、服务转发与网关健康检查 |
| 认证服务（mes-auth） | 8081 | `/api/auth` | 登录、登出、当前用户、权限与基础用户查询 |
| 系统服务（mes-system） | 8082 | `/api/system` | 菜单、编号序列与操作审计日志 |
| 生产服务（mes-production） | 9103 | `/api/production、/api/andon` | 工单、派工、任务、齐套、条码、追溯与电子 SOP |
| 质量服务（mes-quality） | 9104 | `/api/quality` | 检验基础数据、检验单、结果与缺陷闭环 |
| 设备服务（mes-equipment） | 8083 | `/api/equipment` | 设备台账、点检、保养、维修、采集、OEE 与能源数据 |
| 报表服务（mes-report） | 8086 | `/api/report` | 指标、驾驶舱、生产报表与电子看板聚合 |
| 集成服务（mes-integration） | 8087 | `/api/integration、/api/openapi` | ERP、标准 OpenAPI、同步日志与外部系统管理 |

### 1.2 通用参数约定

| 名称 | 位置 | 说明 |
|---|---|---|
| `id` / `*Id` | 路径或请求体 | 数据库主键，必须使用后端返回值，不可用表格序号代替 |
| `resource` | 路径 | 仅允许服务白名单中的资源名，详见设备/集成资源表 |
| `page`, `size` | 查询 | 分页页码和每页数量；具体起始页以接口实际返回为准 |
| `keyword` | 查询或路径 | 模糊搜索关键字；放入路径时必须使用 `encodeURIComponent` |
| `status` | 查询 | 状态筛选；值应取接口返回的状态枚举，不在前端自行翻译后回传 |
| 时间范围 | 查询 | 使用服务 DTO 声明的字段，例如开始/结束日期；同一页面需保持时区一致 |

## 2. 网关服务（mes-gateway）

**职责：** 统一入口、鉴权过滤、服务转发与网关健康检查。直连端口 `8080`，前端统一通过 8080 网关调用。

### /api/gateway/health

#### `GET /api/gateway/health`

- **用途：** 服务存活检查，仅用于部署验证和运维监控。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

## 3. 认证服务（mes-auth）

**职责：** 登录、登出、当前用户、权限与基础用户查询。直连端口 `8081`，前端统一通过 8080 网关调用。

### /api/auth/login

#### `POST /api/auth/login`

- **用途：** 校验账号凭据并签发登录令牌，同时返回当前用户和权限信息。
- **后端返回类型：** `Result<LoginVO>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `LoginRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/auth/logout

#### `POST /api/auth/logout`

- **用途：** 结束当前登录会话；前端成功后应清除本地令牌与用户缓存。
- **后端返回类型：** `Result<Void>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/auth/me

#### `GET /api/auth/me`

- **用途：** 读取令牌对应的当前登录用户信息。
- **后端返回类型：** `Result<LoginVO>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `Authorization` | 请求头 | `String` | 是 | 认证或调用上下文请求头 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/auth/me/permissions`

- **用途：** 读取当前用户的权限集合，用于菜单和按钮级权限控制。
- **后端返回类型：** `Result<LoginVO>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `Authorization` | 请求头 | `String` | 是 | 认证或调用上下文请求头 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/auth/users

#### `GET /api/auth/users`

- **用途：** 查询用户列表。
- **后端返回类型：** `Result<List<UserVO>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

## 4. 系统服务（mes-system）

**职责：** 菜单、编号序列与操作审计日志。直连端口 `8082`，前端统一通过 8080 网关调用。

### /api/system/menus

#### `GET /api/system/menus/tree`

- **用途：** 返回可用于侧边导航的层级菜单树。
- **后端返回类型：** `Result<List<SysMenu>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/system/sequences

#### `POST /api/system/sequences/next`

- **用途：** 按编号规则原子获取下一个业务编号。
- **后端返回类型：** `Result<String>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `SequenceNextRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/system/operation-logs

#### `POST /api/system/operation-logs`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `Result<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `dto` | 请求体 | `OperationLogCreateDTO` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

## 5. 生产服务（mes-production）

**职责：** 工单、派工、任务、齐套、条码、追溯与电子 SOP。直连端口 `9103`，前端统一通过 8080 网关调用。

### /api/andon/exceptions

#### `GET /api/andon/exceptions`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<Map<String, Object>>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/barcodes

#### `GET /api/production/barcodes/types`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<BarcodeType>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/items`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<BarcodeItemOption>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/templates`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<BarcodeTemplate>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `typeId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/rules`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<BarcodeRule>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `typeId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/rules/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<BarcodeRule>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/rules`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<BarcodeRule>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `BarcodeRuleRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/production/barcodes/rules/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<BarcodeRule>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `BarcodeRuleRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/rules/{id}/enable`

- **用途：** 启用配置记录。
- **后端返回类型：** `ApiResponse<BarcodeRule>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/rules/{id}/disable`

- **用途：** 停用配置记录。
- **后端返回类型：** `ApiResponse<BarcodeRule>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/production/barcodes/rules/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/application-rules`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<BarcodeApplicationRule>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `itemId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `typeId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/application-rules`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<BarcodeApplicationRule>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `BarcodeApplicationRuleRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/production/barcodes/application-rules/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<BarcodeApplicationRule>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `BarcodeApplicationRuleRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/production/barcodes/application-rules/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<BarcodeRecord>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `typeId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `workOrderId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `taskId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `batchNo` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<BarcodeRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/generate`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<BarcodeGenerationResult>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `BarcodeGenerateRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/{id}/print`

- **用途：** 登记并执行条码打印；批量路径按请求体中的条码集合处理。
- **后端返回类型：** `ApiResponse<BarcodePrintResult>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `BarcodePrintRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/print`

- **用途：** 登记并执行条码打印；批量路径按请求体中的条码集合处理。
- **后端返回类型：** `ApiResponse<List<BarcodePrintResult>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `BarcodeBatchPrintRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/{id}/print-records`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<List<BarcodePrintRecord>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/barcodes/{id}/events`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<List<BarcodeTraceEvent>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/scan`

- **用途：** 提交扫码事件并返回条码解析、校验和关联业务结果。
- **后端返回类型：** `ApiResponse<BarcodeRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `BarcodeScanRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/{id}/parent`

- **用途：** 建立当前条码与父级包装或产品条码的关系。
- **后端返回类型：** `ApiResponse<BarcodeRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `BarcodeParentBindRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/{id}/close`

- **用途：** 关闭已完成或无需继续处理的业务对象。
- **后端返回类型：** `ApiResponse<BarcodeRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/barcodes/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<BarcodeRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/trace

#### `GET /api/production/trace/{keyword}`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<BarcodeTraceResult>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `SN` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/work-orders

#### `GET /api/production/work-orders`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<WorkOrder>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `productId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `lineId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `sourceType` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `externalNo` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/work-orders/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `WorkOrderRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/production/work-orders/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `WorkOrderRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/production/work-orders/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders/{id}/issue`

- **用途：** 下达业务单据，进入待执行状态。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders/{id}/confirm-issue`

- **用途：** 确认已接收下达的业务单据。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders/{id}/start`

- **用途：** 启动业务处理并推进状态。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders/{id}/pause`

- **用途：** 暂停正在执行的业务对象。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders/{id}/complete`

- **用途：** 提交完成结果并推进至完成状态。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `CompleteWorkOrderRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders/{id}/close`

- **用途：** 关闭已完成或无需继续处理的业务对象。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/work-orders/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<WorkOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/kitting-analyses

#### `GET /api/production/kitting-analyses`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<KittingAnalysis>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `workOrderId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `taskId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/kitting-analyses/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<KittingAnalysis>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/kitting-analyses`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<KittingAnalysis>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `KittingAnalysisRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/production/kitting-analyses/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<KittingAnalysis>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `KittingAnalysisRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/production/kitting-analyses/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/kitting-analyses/{id}/complete`

- **用途：** 提交完成结果并推进至完成状态。
- **后端返回类型：** `ApiResponse<KittingAnalysis>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/kitting-analyses/{id}/close`

- **用途：** 关闭已完成或无需继续处理的业务对象。
- **后端返回类型：** `ApiResponse<KittingAnalysis>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/kitting-analyses/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<KittingAnalysis>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/kitting-analyses/{analysisId}/missing-items`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<KittingMissingItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `analysisId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `KittingMissingItemRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/kitting-missing-items

#### `GET /api/production/kitting-missing-items`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<KittingMissingItem>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `analysisId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `materialId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/kitting-missing-items/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<KittingMissingItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/production/kitting-missing-items/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<KittingMissingItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `KittingMissingItemRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/production/kitting-missing-items/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/kitting-missing-board

#### `GET /api/production/kitting-missing-board`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<KittingMissingItem>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/dispatch-orders

#### `GET /api/production/dispatch-orders`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<DispatchOrder>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `workOrderId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `lineId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `teamId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/dispatch-orders/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `DispatchOrderRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/production/dispatch-orders/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `DispatchOrderRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/production/dispatch-orders/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders/{id}/issue`

- **用途：** 下达业务单据，进入待执行状态。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders/{id}/confirm-issue`

- **用途：** 确认已接收下达的业务单据。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders/{id}/start`

- **用途：** 启动业务处理并推进状态。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders/{id}/complete`

- **用途：** 提交完成结果并推进至完成状态。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders/{id}/close`

- **用途：** 关闭已完成或无需继续处理的业务对象。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<DispatchOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/dispatch-orders/{id}/tasks`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<ShopTask>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `CreateTaskRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/tasks

#### `GET /api/production/tasks`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<ShopTask>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `workOrderId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `dispatchId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `lineId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `teamId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/tasks/{id}/start`

- **用途：** 启动业务处理并推进状态。
- **后端返回类型：** `ApiResponse<ShopTask>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/tasks/{id}/pause`

- **用途：** 暂停正在执行的业务对象。
- **后端返回类型：** `ApiResponse<ShopTask>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/tasks/{id}/resume`

- **用途：** 恢复已暂停的业务对象。
- **后端返回类型：** `ApiResponse<ShopTask>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/tasks/{id}/complete`

- **用途：** 提交完成结果并推进至完成状态。
- **后端返回类型：** `ApiResponse<ShopTask>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `ProductionActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /internal/status

#### `GET /internal/status`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<Map<String, String>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/documents

#### `GET /api/production/sop/documents`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<SopDocument>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `category` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/sop/documents/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/documents`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<SopDocument>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `SopDocumentRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/production/sop/documents/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<SopDocument>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopDocumentRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/sop/documents/{id}/versions`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<List<SopVersion>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/documents/{id}/versions`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopVersionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/versions

#### `POST /api/production/sop/versions/{id}/copy`

- **用途：** 复制现有版本生成新的可编辑版本。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopVersionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/submit`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStatusActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/reject`

- **用途：** 驳回当前版本并记录原因。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStatusActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/approve`

- **用途：** 审批通过当前版本或业务对象。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStatusActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/publish`

- **用途：** 发布已审批版本，使其可供现场执行使用。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStatusActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/disable`

- **用途：** 停用配置记录。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStatusActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<SopVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStatusActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/sop/versions/{id}/steps`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<List<SopStep>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/steps`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<SopStep>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStepRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/sop/versions/{id}/attachments`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<List<SopAttachment>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/attachments`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<SopAttachment>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `stepId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `displayOrder` | 查询 | `Integer` | 否 | 可选筛选或控制参数 |
  | `file` | 查询对象 | `MultipartFile` | 是 | 按该查询 DTO 的字段展开为 URL 查询参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/models`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<SopModelVersion>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `operatorId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `file` | 查询对象 | `MultipartFile` | 是 | 按该查询 DTO 的字段展开为 URL 查询参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/sop/versions/{id}/bindings`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<List<SopBinding>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/versions/{id}/bindings`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<SopBinding>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopBindingRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/steps

#### `PUT /api/production/sop/steps/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<SopStep>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStepRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/production/sop/steps/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/attachments

#### `DELETE /api/production/sop/attachments/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/bindings

#### `DELETE /api/production/sop/bindings/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/tasks

#### `POST /api/production/sop/tasks/{taskId}/lock`

- **用途：** 为生产任务锁定适用的 SOP 版本并生成执行快照。
- **后端返回类型：** `ApiResponse<SopTaskSnapshot>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `taskId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `operatorId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/sop/tasks/{taskId}`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<SopTaskPackage>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `taskId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `operatorId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/production/sop/tasks/{taskId}/report-validation`

- **用途：** 校验任务报工前必做 SOP 步骤是否全部完成。
- **后端返回类型：** `ApiResponse<SopReportValidationResult>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `taskId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/snapshots

#### `POST /api/production/sop/snapshots/{snapshotId}/steps/{stepId}/view`

- **用途：** 记录现场人员已查看指定 SOP 步骤。
- **后端返回类型：** `ApiResponse<SopStepExecutionRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `snapshotId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `stepId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `operatorId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/production/sop/snapshots/{snapshotId}/steps/{stepId}/confirm`

- **用途：** 确认指定 SOP 步骤已按要求执行。
- **后端返回类型：** `ApiResponse<SopStepExecutionRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `snapshotId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `stepId` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `SopStepExecutionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/executions

#### `GET /api/production/sop/executions`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<SopExecutionRecord>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `taskId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `versionId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/files

#### `GET /api/production/sop/files/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ResponseEntity<Resource>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/production/sop/model-files

#### `GET /api/production/sop/model-files/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ResponseEntity<Resource>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

## 6. 质量服务（mes-quality）

**职责：** 检验基础数据、检验单、结果与缺陷闭环。直连端口 `9104`，前端统一通过 8080 网关调用。

### /api/quality/item-categories

#### `GET /api/quality/item-categories`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<QcItemCategory>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/quality/item-categories/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<QcItemCategory>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/item-categories`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<QcItemCategory>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `QcItemCategoryRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/quality/item-categories/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<QcItemCategory>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QcItemCategoryRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/quality/item-categories/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/item-categories/{id}/enable`

- **用途：** 启用配置记录。
- **后端返回类型：** `ApiResponse<QcItemCategory>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/item-categories/{id}/disable`

- **用途：** 停用配置记录。
- **后端返回类型：** `ApiResponse<QcItemCategory>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/item-categories/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<QcItemCategory>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/quality/items

#### `GET /api/quality/items`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<QcItem>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `categoryId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/quality/items/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<QcItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/items`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<QcItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `QcItemRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/quality/items/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<QcItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QcItemRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/quality/items/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/items/{id}/enable`

- **用途：** 启用配置记录。
- **后端返回类型：** `ApiResponse<QcItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/items/{id}/disable`

- **用途：** 停用配置记录。
- **后端返回类型：** `ApiResponse<QcItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/items/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<QcItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/quality/plans

#### `GET /api/quality/plans`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<QcPlan>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `productId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `customerId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/quality/plans/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<QcPlan>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/plans`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<QcPlan>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `QcPlanRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/quality/plans/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<QcPlan>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QcPlanRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/quality/plans/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/plans/{id}/enable`

- **用途：** 启用配置记录。
- **后端返回类型：** `ApiResponse<QcPlan>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/plans/{id}/disable`

- **用途：** 停用配置记录。
- **后端返回类型：** `ApiResponse<QcPlan>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/plans/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<QcPlan>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/quality/plan-items

#### `GET /api/quality/plan-items`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<QcPlanItem>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `planId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `qcItemId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/quality/plan-items/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<QcPlanItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/plan-items`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<QcPlanItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `QcPlanItemRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/quality/plan-items/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<QcPlanItem>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QcPlanItemRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/quality/plan-items/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/quality/inspection-orders

#### `GET /api/quality/inspection-orders`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<QcInspectionOrder>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `keyword` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `inspectionType` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `planId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `taskId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `operationTaskId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `barcodeId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/quality/inspection-orders/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/inspection-orders`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `QcInspectionOrderRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/quality/inspection-orders/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QcInspectionOrderRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/quality/inspection-orders/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/inspection-orders/{id}/start`

- **用途：** 启动业务处理并推进状态。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/inspection-orders/{id}/pass`

- **用途：** 提交检验合格判定并推进检验单状态。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/inspection-orders/{id}/fail`

- **用途：** 提交检验不合格判定并进入缺陷处理流程。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/inspection-orders/{id}/close`

- **用途：** 关闭已完成或无需继续处理的业务对象。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/inspection-orders/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<QcInspectionOrder>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/quality/inspection-results

#### `GET /api/quality/inspection-results`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<QcInspectionResult>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `inspectionId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `qcItemId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `result` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/quality/inspection-results/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<QcInspectionResult>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/inspection-results`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<QcInspectionResult>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `QcInspectionResultRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/quality/inspection-results/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<QcInspectionResult>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QcInspectionResultRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/quality/inspection-results/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/quality/defect-records

#### `GET /api/quality/defect-records`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<List<QcDefectRecord>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `sourceType` | 查询 | `String` | 否 | 可选筛选或控制参数 |
  | `sourceId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `productId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `barcodeId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `processId` | 查询 | `Long` | 否 | 可选筛选或控制参数 |
  | `status` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/quality/defect-records/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<QcDefectRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/defect-records`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<QcDefectRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `request` | 请求体 | `QcDefectRecordRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/quality/defect-records/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<QcDefectRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QcDefectRecordRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `DELETE /api/quality/defect-records/{id}`

- **用途：** 删除尚未进入业务流转的记录；已被引用或已执行的记录通常应改用作废接口。
- **后端返回类型：** `ApiResponse<Void>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/defect-records/{id}/start`

- **用途：** 启动业务处理并推进状态。
- **后端返回类型：** `ApiResponse<QcDefectRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/defect-records/{id}/complete`

- **用途：** 提交完成结果并推进至完成状态。
- **后端返回类型：** `ApiResponse<QcDefectRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/defect-records/{id}/close`

- **用途：** 关闭已完成或无需继续处理的业务对象。
- **后端返回类型：** `ApiResponse<QcDefectRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/quality/defect-records/{id}/void`

- **用途：** 作废业务对象并保留审计记录。
- **后端返回类型：** `ApiResponse<QcDefectRecord>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `Long` | 是 | 资源定位参数，调用前必须存在 |
  | `request` | 请求体 | `QualityActionRequest` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /internal/status

#### `GET /internal/status`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<Map<String, String>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

## 7. 设备服务（mes-equipment）

**职责：** 设备台账、点检、保养、维修、采集、OEE 与能源数据。直连端口 `8083`，前端统一通过 8080 网关调用。

### /api/equipment/{resource}

#### `GET /api/equipment/{resource}`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `params` | 查询 | `Map<String, String>` | 是 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/equipment/{resource}/{id}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/equipment/{resource}`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/equipment/{resource}/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/equipment/{resource}/{id}/{action}`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |
  | `action` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/equipment/equipments

#### `POST /api/equipment/equipments/{id}/running-status`

- **用途：** 更新设备实时运行状态。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/mes/{func}

#### `GET /api/mes/{func}`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `func` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `params` | 查询 | `Map<String, String>` | 是 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/mes/{func}`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `func` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/mes/{func}/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `func` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/equipment/health

#### `GET /api/equipment/health`

- **用途：** 服务存活检查，仅用于部署验证和运维监控。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### 设备动态资源白名单

动态 CRUD 路径中的 `{resource}` 只能取下表值。GET 列表支持以可筛选字段作为查询参数；POST/PUT 请求体不得提交表外字段。

| resource | 业务含义 | 主要字段 |
|---|---|---|
| `categories` | 设备类别 | `category_code, category_name, status` |
| `manufacturers` | 设备制造商 | `manufacturer_code, manufacturer_name, contact, status` |
| `fault-reasons` | 故障原因 | `reason_code, reason_name, category_id, status` |
| `equipments` | 设备台账 | `equipment_code, equipment_name, category_id, manufacturer_id, line_id, station_id, model, serial_no, purchase_date, equipment_status, status` |
| `maintenance-tasks` | 保养任务 | `maintenance_no, equipment_id, plan_at, assigned_to, completed_at, result, status` |
| `inspections` | 设备点检记录 | `inspection_no, equipment_id, inspector_id, inspection_at, result, abnormal_desc, status` |
| `repairs` | 维修工单 | `repair_no, equipment_id, fault_reason_id, reported_by, reported_at, assigned_to, repair_start_at, repair_end_at, repair_result, status` |
| `device-points` | 设备采集点 | `equipment_id, point_code, point_name, data_type, unit_id, status` |
| `collect-records` | 采集记录 | `equipment_id, point_id, collect_value, collect_at, quality_flag` |
| `count-reports` | 设备计数报工 | `equipment_id, task_id, operation_task_id, count_qty, report_at, sync_log_id` |
| `debug-logs` | 设备调试日志 | `equipment_id, request_payload, response_payload, result, debug_at` |
| `oee-snapshots` | OEE 快照 | `equipment_id, line_id, stat_date, availability, performance, quality_rate, oee` |
| `energy-records` | 能源记录 | `equipment_id, energy_type, value, unit_id, record_at` |

通用动作 `POST /api/equipment/{resource}/{id}/{action}` 支持：`submit→PROCESSING`、`confirm/complete→COMPLETED`、`close→CLOSED`、`cancel→CANCELLED`、`enable→ENABLED`、`disable→DISABLED`。并非每种资源都适合每个动作，前端应只展示业务允许的动作。

## 8. 报表服务（mes-report）

**职责：** 指标、驾驶舱、生产报表与电子看板聚合。直连端口 `8086`，前端统一通过 8080 网关调用。

### /api/report/metrics

#### `GET /api/report/metrics`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<MetricDef>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `metricType` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/report/metric-snapshots

#### `GET /api/report/metric-snapshots`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<MetricSnapshot>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `query` | 查询对象 | `MetricQuery` | 是 | 按该查询 DTO 的字段展开为 URL 查询参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/report/boards

#### `GET /api/report/boards`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<BoardConfig>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `boardType` | 查询 | `String` | 否 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/report/boards/line`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<Map<String, Object>>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/report/boards/workshop`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<Map<String, Object>>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/report/boards/control-center`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<Map<String, Object>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/report/boards/line-config`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<BoardConfig>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/report/boards/workshop-config`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<BoardConfig>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/report/metric-data

#### `GET /api/report/metric-data/{metricCode}`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<ReportDatasetVO>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `metricCode` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/report/dashboard

#### `GET /api/report/dashboard/manufacturing`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<ManufacturingDashboardVO>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/report/production-output

#### `GET /api/report/production-output`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `Result<List<MetricSnapshot>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `query` | 查询对象 | `MetricQuery` | 是 | 按该查询 DTO 的字段展开为 URL 查询参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/report/production-output/daily`

- **用途：** 查询按日汇总的生产产量数据。
- **后端返回类型：** `Result<List<DailyOutputReportVO>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

## 9. 集成服务（mes-integration）

**职责：** ERP、标准 OpenAPI、同步日志与外部系统管理。直连端口 `8087`，前端统一通过 8080 网关调用。

### /api/integration/{resource}

#### `GET /api/integration/{resource}`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `params` | 查询 | `Map<String, String>` | 是 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `GET /api/integration/{resource}/{id:\\d+}`

- **用途：** 按主键读取单条记录详情；记录不存在时返回业务错误。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/integration/{resource}`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `PUT /api/integration/{resource}/{id}`

- **用途：** 按主键更新可编辑字段；状态流转应使用专用动作接口而非直接修改状态。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/integration/{resource}/{id}/{action}`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `resource` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `id` | 路径 | `long` | 是 | 资源定位参数，调用前必须存在 |
  | `action` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/integration/erp

#### `GET /api/integration/erp/sync-logs`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `params` | 查询 | `Map<String, String>` | 是 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/integration/erp/production-tasks/read`

- **用途：** 接收外部系统业务报文，建立同步日志并执行幂等处理；结果不等同于下游业务必然成功。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/integration/erp/routes/read`

- **用途：** 接收外部系统业务报文，建立同步日志并执行幂等处理；结果不等同于下游业务必然成功。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/openapi/units

#### `POST /api/openapi/units/write`

- **用途：** 接收外部系统业务报文，建立同步日志并执行幂等处理；结果不等同于下游业务必然成功。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/openapi/work-orders

#### `POST /api/openapi/work-orders/write`

- **用途：** 接收外部系统业务报文，建立同步日志并执行幂等处理；结果不等同于下游业务必然成功。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/openapi/production-tasks

#### `POST /api/openapi/production-tasks/write`

- **用途：** 接收外部系统业务报文，建立同步日志并执行幂等处理；结果不等同于下游业务必然成功。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/openapi/device-count-reports

#### `POST /api/openapi/device-count-reports/write`

- **用途：** 接收外部系统业务报文，建立同步日志并执行幂等处理；结果不等同于下游业务必然成功。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/openapi/completion-orders

#### `POST /api/openapi/completion-orders/read`

- **用途：** 接收外部系统业务报文，建立同步日志并执行幂等处理；结果不等同于下游业务必然成功。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/mes/{func}

#### `GET /api/mes/{func}`

- **用途：** 按查询条件读取列表或聚合数据；支持的筛选字段见本节参数说明。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `func` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `params` | 查询 | `Map<String, String>` | 是 | 可选筛选或控制参数 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

#### `POST /api/mes/{func}`

- **用途：** 创建业务记录或执行指定业务动作；成功后返回最新业务数据。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：**

  | 参数 | 位置 | 类型 | 必填 | 说明 |
  |---|---|---|---|---|
  | `func` | 路径 | `String` | 是 | 资源定位参数，调用前必须存在 |
  | `body` | 请求体 | `Map<String, Object>` | 是 | JSON 业务对象；字段必须遵循对应 DTO/实体或资源字段约束 |

- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### /api/integration/health

#### `GET /api/integration/health`

- **用途：** 服务存活检查，仅用于部署验证和运维监控。
- **后端返回类型：** `ApiResponse<Map<String, Object>>`。
- **参数：** 无。
- **前端处理：** 成功后使用响应 `data` 更新页面；动作接口需防止重复点击，失败时保留当前表单或列表状态并展示后端消息。

### 集成动态资源白名单

| resource | 业务含义 | 主要字段 |
|---|---|---|
| `external-systems` | 外部系统配置 | `system_code, system_name, auth_type, status` |
| `sync-logs` | 接口同步日志 | `system_id, interface_code, direction, biz_type, biz_id, external_no, idempotent_key, request_payload, response_payload, sync_status, retry_count, error_message, started_at, finished_at` |
| `request-logs` | 请求日志 | `sync_log_id, method, url, headers_json, body, requested_at` |
| `response-logs` | 响应日志 | `sync_log_id, http_status, body, responded_at, elapsed_ms` |

同步接口必须传稳定的 `idempotent_key` 或可推导唯一业务键，避免网络重试生成重复工单。`retry` 仅用于失败同步日志；前端应展示重试次数和最后错误信息。

## 10. 当前前后端差异与接入提醒

1. 前端 `src/api/report.js` 中的 `GET /api/report/quality`、`GET /api/report/equipment` 当前没有对应 Controller，属于 TODO，页面不能按已实现接口处理。
2. 前端 `src/api/system.js` 中的 `GET /api/system/users`、`roles`、`permissions` 当前未由 mes-system 实现；用户列表暂由 `GET /api/auth/users` 提供。
3. 设备与集成服务使用动态资源 Controller。接口文档中的通用 CRUD 只表示路由能力，实际字段、筛选项和动作必须遵守白名单表。
4. `/api/mes/func-*` 是旧版兼容路径，新前端不得新增依赖；新代码统一使用语义化的 `/api/equipment`、`/api/integration`、`/api/openapi` 路径。
5. `/internal/status` 属于服务内部探针，不应从业务页面调用，也不应作为前端功能接口展示。

## 11. 前端封装建议

- 每个微服务保留独立 API 模块，函数名采用“动词 + 业务对象”，例如 `getWorkOrders`、`startWorkOrder`。
- 页面组件只调用 API 函数，不直接拼接 URL；路径参数统一编码，查询参数交给 Axios `params` 处理。
- 列表请求支持取消旧请求，避免快速筛选时旧响应覆盖新结果。
- 动作按钮根据当前状态显示，并在提交期间禁用；成功后优先用后端返回的最新对象更新行数据。
- 上传接口使用 `FormData`；下载/模型文件接口按 Blob 或可访问资源响应处理。
- 建议后续补充 OpenAPI 3 描述与枚举 Schema，使本清单可自动生成 TypeScript 类型和 API Client。

## 12. 类型字段速查

以下字段直接提取自当前后端领域模型。请求 DTO 表示前端可提交字段；VO/实体主要用于识别响应结构。数据库通用字段、校验规则和状态枚举仍以后端实现为准。

### `BarcodeApplicationRule`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeApplicationRule.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `applicationRuleCode` | `String` | `string / object` |
| `itemId` | `Long` | `number` |
| `itemCode` | `String` | `string / object` |
| `itemName` | `String` | `string / object` |
| `typeId` | `Long` | `number` |
| `typeCode` | `String` | `string / object` |
| `typeName` | `String` | `string / object` |
| `ruleId` | `Long` | `number` |
| `ruleCode` | `String` | `string / object` |
| `ruleName` | `String` | `string / object` |
| `ruleExpression` | `String` | `string / object` |
| `serialLength` | `Integer` | `number` |
| `templateId` | `Long` | `number` |
| `templateCode` | `String` | `string / object` |
| `templateName` | `String` | `string / object` |
| `barcodeMode` | `String` | `string / object` |
| `sourceType` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `BarcodeGenerationResult`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeGenerationResult.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `quantity` | `int` | `number` |
| `barcodes` | `List<BarcodeRecord>` | `array` |

### `BarcodeItemOption`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeItemOption.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `itemCode` | `String` | `string / object` |
| `itemName` | `String` | `string / object` |
| `itemType` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `BarcodePrintRecord`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodePrintRecord.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `barcodeId` | `Long` | `number` |
| `barcodeValue` | `String` | `string / object` |
| `templateId` | `Long` | `number` |
| `templateName` | `String` | `string / object` |
| `printBy` | `Long` | `number` |
| `printByName` | `String` | `string / object` |
| `printAt` | `LocalDateTime` | `string (ISO-8601)` |
| `printCount` | `Integer` | `number` |
| `printerName` | `String` | `string / object` |

### `BarcodePrintResult`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodePrintResult.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `barcode` | `BarcodeRecord` | `string / object` |
| `printRecord` | `BarcodePrintRecord` | `string / object` |
| `template` | `BarcodeTemplate` | `string / object` |

### `BarcodeRecord`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeRecord.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `barcodeValue` | `String` | `string / object` |
| `typeId` | `Long` | `number` |
| `typeCode` | `String` | `string / object` |
| `typeName` | `String` | `string / object` |
| `applicationRuleId` | `Long` | `number` |
| `applicationRuleCode` | `String` | `string / object` |
| `itemId` | `Long` | `number` |
| `itemCode` | `String` | `string / object` |
| `itemName` | `String` | `string / object` |
| `batchNo` | `String` | `string / object` |
| `workOrderId` | `Long` | `number` |
| `workOrderNo` | `String` | `string / object` |
| `taskId` | `Long` | `number` |
| `taskNo` | `String` | `string / object` |
| `parentBarcodeId` | `Long` | `number` |
| `parentBarcodeValue` | `String` | `string / object` |
| `printCount` | `Integer` | `number` |
| `sourceType` | `String` | `string / object` |
| `status` | `String` | `string / object` |
| `generatedAt` | `LocalDateTime` | `string (ISO-8601)` |

### `BarcodeRule`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeRule.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `ruleCode` | `String` | `string / object` |
| `ruleName` | `String` | `string / object` |
| `typeId` | `Long` | `number` |
| `typeCode` | `String` | `string / object` |
| `typeName` | `String` | `string / object` |
| `ruleExpression` | `String` | `string / object` |
| `serialLength` | `Integer` | `number` |
| `status` | `String` | `string / object` |
| `applicationRuleId` | `Long` | `number` |
| `applicationRuleCode` | `String` | `string / object` |
| `itemId` | `Long` | `number` |
| `itemCode` | `String` | `string / object` |
| `itemName` | `String` | `string / object` |
| `templateId` | `Long` | `number` |
| `templateCode` | `String` | `string / object` |
| `templateName` | `String` | `string / object` |
| `barcodeMode` | `String` | `string / object` |
| `sourceType` | `String` | `string / object` |

### `BarcodeTemplate`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeTemplate.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `templateCode` | `String` | `string / object` |
| `templateName` | `String` | `string / object` |
| `typeId` | `Long` | `number` |
| `typeCode` | `String` | `string / object` |
| `typeName` | `String` | `string / object` |
| `templateContent` | `String` | `string / object` |
| `paperWidth` | `BigDecimal` | `number` |
| `paperHeight` | `BigDecimal` | `number` |
| `status` | `String` | `string / object` |

### `BarcodeTraceEvent`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeTraceEvent.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `barcodeId` | `Long` | `number` |
| `barcode` | `String` | `string / object` |
| `eventType` | `String` | `string / object` |
| `bizType` | `String` | `string / object` |
| `bizId` | `Long` | `number` |
| `workOrderId` | `Long` | `number` |
| `workOrderNo` | `String` | `string / object` |
| `taskId` | `Long` | `number` |
| `taskNo` | `String` | `string / object` |
| `processId` | `Long` | `number` |
| `processName` | `String` | `string / object` |
| `stationId` | `Long` | `number` |
| `stationName` | `String` | `string / object` |
| `deviceId` | `Long` | `number` |
| `equipmentName` | `String` | `string / object` |
| `operatorId` | `Long` | `number` |
| `operatorName` | `String` | `string / object` |
| `eventAt` | `LocalDateTime` | `string (ISO-8601)` |
| `result` | `String` | `string / object` |

### `BarcodeTraceNode`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeTraceNode.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `String` | `string / object` |
| `parentId` | `String` | `string / object` |
| `barcode` | `String` | `string / object` |
| `title` | `String` | `string / object` |
| `relation` | `String` | `string / object` |
| `type` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `BarcodeTraceResult`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeTraceResult.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `root` | `String` | `string / object` |
| `queryMode` | `String` | `string / object` |
| `nodes` | `List<BarcodeTraceNode>` | `array` |
| `details` | `Map<String, Object>` | `object` |
| `events` | `List<BarcodeTraceEvent>` | `array` |

### `BarcodeType`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/barcode/barcode/BarcodeType.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `typeCode` | `String` | `string / object` |
| `typeName` | `String` | `string / object` |
| `uniqueScope` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `BoardConfig`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/entity/BoardConfig.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `boardCode` | `String` | `string / object` |
| `boardName` | `String` | `string / object` |
| `boardType` | `String` | `string / object` |
| `refreshSeconds` | `Integer` | `number` |
| `configJson` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `DailyOutputReportVO`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/vo/DailyOutputReportVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `statDate` | `LocalDate` | `string (ISO-8601)` |
| `lineCode` | `String` | `string / object` |
| `lineName` | `String` | `string / object` |
| `workOrderNo` | `String` | `string / object` |
| `productCode` | `String` | `string / object` |
| `productName` | `String` | `string / object` |
| `plannedQty` | `BigDecimal` | `number` |
| `reportedQty` | `BigDecimal` | `number` |
| `qualifiedQty` | `BigDecimal` | `number` |
| `defectiveQty` | `BigDecimal` | `number` |
| `reportCount` | `Integer` | `number` |

### `DashboardMetricVO`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/vo/DashboardMetricVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `metricKey` | `String` | `string / object` |
| `value` | `BigDecimal` | `number` |

### `DashboardStockVO`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/vo/DashboardStockVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `materialCode` | `String` | `string / object` |
| `materialName` | `String` | `string / object` |
| `unitName` | `String` | `string / object` |
| `requiredQty` | `BigDecimal` | `number` |
| `actualQty` | `BigDecimal` | `number` |
| `stockStatus` | `String` | `string / object` |

### `DispatchOrder`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/DispatchOrder.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `dispatchNo` | `String` | `string / object` |
| `workOrderId` | `Long` | `number` |
| `lineId` | `Long` | `number` |
| `stationId` | `Long` | `number` |
| `teamId` | `Long` | `number` |
| `planQty` | `BigDecimal` | `number` |
| `plannedStartAt` | `LocalDateTime` | `string (ISO-8601)` |
| `plannedEndAt` | `LocalDateTime` | `string (ISO-8601)` |
| `status` | `String` | `string / object` |

### `KittingAnalysis`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/KittingAnalysis.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `analysisNo` | `String` | `string / object` |
| `workOrderId` | `Long` | `number` |
| `taskId` | `Long` | `number` |
| `analysisTime` | `LocalDateTime` | `string (ISO-8601)` |
| `kittingStatus` | `String` | `string / object` |
| `missingCount` | `Integer` | `number` |
| `status` | `String` | `string / object` |

### `KittingMissingItem`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/KittingMissingItem.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `analysisId` | `Long` | `number` |
| `materialId` | `Long` | `number` |
| `requiredQty` | `BigDecimal` | `number` |
| `availableQty` | `BigDecimal` | `number` |
| `missingQty` | `BigDecimal` | `number` |
| `expectedArrivalAt` | `LocalDateTime` | `string (ISO-8601)` |

### `LoginRequest`

来源：`backend/mes-auth/src/main/java/com/fanmes/auth/domain/dto/LoginRequest.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `username` | `String` | `string / object` |
| `password` | `String` | `string / object` |

### `LoginVO`

来源：`backend/mes-auth/src/main/java/com/fanmes/auth/domain/vo/LoginVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `accessToken` | `String` | `string / object` |
| `tokenType` | `String` | `string / object` |
| `userId` | `Long` | `number` |
| `username` | `String` | `string / object` |
| `displayName` | `String` | `string / object` |
| `roles` | `Set<String>` | `array` |
| `permissions` | `Set<String>` | `array` |

### `ManufacturingDashboardVO`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/vo/ManufacturingDashboardVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `lines` | `List<ManufacturingLineVO>` | `array` |
| `gauges` | `List<DashboardMetricVO>` | `array` |
| `stock` | `List<DashboardStockVO>` | `array` |

### `ManufacturingLineVO`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/vo/ManufacturingLineVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `lineId` | `Long` | `number` |
| `lineCode` | `String` | `string / object` |
| `lineName` | `String` | `string / object` |
| `batchNo` | `String` | `string / object` |
| `workOrderNo` | `String` | `string / object` |
| `productName` | `String` | `string / object` |
| `plannedQty` | `BigDecimal` | `number` |
| `completedQty` | `BigDecimal` | `number` |
| `goodQty` | `BigDecimal` | `number` |
| `defectQty` | `BigDecimal` | `number` |
| `completionRate` | `BigDecimal` | `number` |
| `oee` | `BigDecimal` | `number` |
| `performance` | `BigDecimal` | `number` |
| `outputTrend` | `List<Integer>` | `array` |
| `runningOrderCount` | `Integer` | `number` |
| `active` | `boolean` | `boolean` |

### `MetricDef`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/entity/MetricDef.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `metricCode` | `String` | `string / object` |
| `metricName` | `String` | `string / object` |
| `metricType` | `String` | `string / object` |
| `calcExpression` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `MetricQuery`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/dto/MetricQuery.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `metricId` | `Long` | `number` |
| `metricType` | `String` | `string / object` |
| `statDimension` | `String` | `string / object` |
| `dimensionId` | `Long` | `number` |

### `MetricSnapshot`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/entity/MetricSnapshot.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `metricId` | `Long` | `number` |
| `statDimension` | `String` | `string / object` |
| `dimensionId` | `Long` | `number` |
| `statPeriod` | `String` | `string / object` |
| `statTime` | `LocalDateTime` | `string (ISO-8601)` |
| `metricValue` | `BigDecimal` | `number` |

### `OperationLogCreateDTO`

来源：`backend/mes-system/src/main/java/com/fanmes/system/domain/dto/OperationLogCreateDTO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `bizType` | `String` | `string / object` |
| `bizId` | `Long` | `number` |
| `action` | `String` | `string / object` |
| `requestBody` | `String` | `string / object` |
| `operatorId` | `Long` | `number` |
| `result` | `String` | `string / object` |
| `errorMessage` | `String` | `string / object` |

### `QcDefectRecord`

来源：`backend/mes-quality/src/main/java/com/fanmes/quality/domain/QcDefectRecord.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `sourceType` | `String` | `string / object` |
| `sourceId` | `Long` | `number` |
| `productId` | `Long` | `number` |
| `barcodeId` | `Long` | `number` |
| `processId` | `Long` | `number` |
| `defectReasonId` | `Long` | `number` |
| `defectReasonName` | `String` | `string / object` |
| `defectQty` | `BigDecimal` | `number` |
| `handleMethod` | `String` | `string / object` |
| `reworkOrderId` | `Long` | `number` |
| `status` | `String` | `string / object` |

### `QcInspectionOrder`

来源：`backend/mes-quality/src/main/java/com/fanmes/quality/domain/QcInspectionOrder.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `inspectionNo` | `String` | `string / object` |
| `inspectionType` | `String` | `string / object` |
| `planId` | `Long` | `number` |
| `workOrderId` | `Long` | `number` |
| `workOrderNo` | `String` | `string / object` |
| `taskId` | `Long` | `number` |
| `operationTaskId` | `Long` | `number` |
| `productId` | `Long` | `number` |
| `productName` | `String` | `string / object` |
| `barcodeId` | `Long` | `number` |
| `inspectorId` | `Long` | `number` |
| `inspectionAt` | `LocalDateTime` | `string (ISO-8601)` |
| `finalResult` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `QcInspectionResult`

来源：`backend/mes-quality/src/main/java/com/fanmes/quality/domain/QcInspectionResult.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `inspectionId` | `Long` | `number` |
| `qcItemId` | `Long` | `number` |
| `measuredValue` | `String` | `string / object` |
| `result` | `String` | `string / object` |
| `defectReasonId` | `Long` | `number` |
| `remark` | `String` | `string / object` |

### `QcItem`

来源：`backend/mes-quality/src/main/java/com/fanmes/quality/domain/QcItem.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `categoryId` | `Long` | `number` |
| `itemCode` | `String` | `string / object` |
| `itemName` | `String` | `string / object` |
| `valueType` | `String` | `string / object` |
| `standardValue` | `String` | `string / object` |
| `upperLimit` | `BigDecimal` | `number` |
| `lowerLimit` | `BigDecimal` | `number` |
| `unitId` | `Long` | `number` |
| `status` | `String` | `string / object` |

### `QcItemCategory`

来源：`backend/mes-quality/src/main/java/com/fanmes/quality/domain/QcItemCategory.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `categoryCode` | `String` | `string / object` |
| `categoryName` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `QcPlan`

来源：`backend/mes-quality/src/main/java/com/fanmes/quality/domain/QcPlan.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `planCode` | `String` | `string / object` |
| `planName` | `String` | `string / object` |
| `productId` | `Long` | `number` |
| `customerId` | `Long` | `number` |
| `defaultFlag` | `Boolean` | `boolean` |
| `status` | `String` | `string / object` |

### `QcPlanItem`

来源：`backend/mes-quality/src/main/java/com/fanmes/quality/domain/QcPlanItem.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `planId` | `Long` | `number` |
| `qcItemId` | `Long` | `number` |
| `sampleQty` | `BigDecimal` | `number` |
| `standardValue` | `String` | `string / object` |
| `upperLimit` | `BigDecimal` | `number` |
| `lowerLimit` | `BigDecimal` | `number` |
| `requiredFlag` | `Boolean` | `boolean` |

### `ReportDatasetVO`

来源：`backend/mes-report/src/main/java/com/fanmes/report/domain/vo/ReportDatasetVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `metricCode` | `String` | `string / object` |
| `columns` | `List<ColumnVO>` | `array` |
| `rows` | `List<Map<String, Object>>` | `array` |
| `key` | `String` | `string / object` |
| `label` | `String` | `string / object` |

### `Result`

来源：`backend/mes-common/src/main/java/com/fanmes/common/api/Result.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `code` | `String` | `string / object` |
| `message` | `String` | `string / object` |
| `data` | `T` | `string / object` |

### `SequenceNextRequest`

来源：`backend/mes-system/src/main/java/com/fanmes/system/domain/dto/SequenceNextRequest.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `ruleCode` | `String` | `string / object` |
| `bizDate` | `LocalDate` | `string (ISO-8601)` |

### `SequenceRule`

来源：`backend/mes-system/src/main/java/com/fanmes/system/domain/entity/SequenceRule.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `ruleCode` | `String` | `string / object` |
| `bizType` | `String` | `string / object` |
| `prefix` | `String` | `string / object` |
| `datePattern` | `String` | `string / object` |
| `serialLength` | `Integer` | `number` |
| `resetCycle` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `ShopTask`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/ShopTask.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `taskNo` | `String` | `string / object` |
| `workOrderId` | `Long` | `number` |
| `dispatchId` | `Long` | `number` |
| `productId` | `Long` | `number` |
| `routeId` | `Long` | `number` |
| `lineId` | `Long` | `number` |
| `teamId` | `Long` | `number` |
| `planQty` | `BigDecimal` | `number` |
| `startedAt` | `LocalDateTime` | `string (ISO-8601)` |
| `endedAt` | `LocalDateTime` | `string (ISO-8601)` |
| `status` | `String` | `string / object` |
| `otherwise` | `// display names populated by joined list queries (null` | `string / object` |

### `SopAttachment`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopAttachment.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `versionId` | `Long` | `number` |
| `stepId` | `Long` | `number` |
| `attachmentType` | `String` | `string / object` |
| `fileName` | `String` | `string / object` |
| `contentType` | `String` | `string / object` |
| `fileSize` | `Long` | `number` |
| `objectKey` | `String` | `string / object` |
| `fileUrl` | `String` | `string / object` |
| `sha256` | `String` | `string / object` |
| `displayOrder` | `Integer` | `number` |
| `createdAt` | `LocalDateTime` | `string (ISO-8601)` |

### `SopBinding`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopBinding.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `versionId` | `Long` | `number` |
| `bindingType` | `String` | `string / object` |
| `productId` | `Long` | `number` |
| `routeId` | `Long` | `number` |
| `routeStepId` | `Long` | `number` |
| `processId` | `Long` | `number` |
| `workstationId` | `Long` | `number` |
| `equipmentId` | `Long` | `number` |
| `taskId` | `Long` | `number` |
| `priority` | `Integer` | `number` |
| `confirmMode` | `String` | `string / object` |
| `effectiveFrom` | `LocalDateTime` | `string (ISO-8601)` |
| `effectiveTo` | `LocalDateTime` | `string (ISO-8601)` |
| `status` | `String` | `string / object` |
| `createdAt` | `LocalDateTime` | `string (ISO-8601)` |
| `updatedAt` | `LocalDateTime` | `string (ISO-8601)` |

### `SopDocument`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopDocument.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `sopCode` | `String` | `string / object` |
| `sopName` | `String` | `string / object` |
| `category` | `String` | `string / object` |
| `ownerId` | `Long` | `number` |
| `status` | `String` | `string / object` |
| `currentVersionId` | `Long` | `number` |
| `createdAt` | `LocalDateTime` | `string (ISO-8601)` |
| `updatedAt` | `LocalDateTime` | `string (ISO-8601)` |

### `SopExecutionRecord`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopExecutionRecord.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `snapshotId` | `Long` | `number` |
| `taskId` | `Long` | `number` |
| `sopId` | `Long` | `number` |
| `versionId` | `Long` | `number` |
| `operatorId` | `Long` | `number` |
| `openedAt` | `LocalDateTime` | `string (ISO-8601)` |
| `completedAt` | `LocalDateTime` | `string (ISO-8601)` |
| `status` | `String` | `string / object` |

### `SopMatchCandidate`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopMatchCandidate.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `document` | `SopDocument` | `string / object` |
| `version` | `SopVersion` | `string / object` |
| `binding` | `SopBinding` | `string / object` |

### `SopModel`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopModel.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `modelCode` | `String` | `string / object` |
| `modelName` | `String` | `string / object` |
| `status` | `String` | `string / object` |
| `createdAt` | `LocalDateTime` | `string (ISO-8601)` |
| `updatedAt` | `LocalDateTime` | `string (ISO-8601)` |

### `SopModelVersion`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopModelVersion.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `modelId` | `Long` | `number` |
| `versionNo` | `String` | `string / object` |
| `fileName` | `String` | `string / object` |
| `objectKey` | `String` | `string / object` |
| `fileUrl` | `String` | `string / object` |
| `sha256` | `String` | `string / object` |
| `fileSize` | `Long` | `number` |
| `status` | `String` | `string / object` |
| `createdAt` | `LocalDateTime` | `string (ISO-8601)` |

### `SopStatuses`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopStatuses.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `EDITABLE_VERSION_STATUSES` | `static final Set<String>` | `string / object` |
| `RELEASED_VERSION_STATUSES` | `static final Set<String>` | `string / object` |

### `SopStep`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopStep.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `versionId` | `Long` | `number` |
| `stepNo` | `Integer` | `number` |
| `stepName` | `String` | `string / object` |
| `instruction` | `String` | `string / object` |
| `contentType` | `String` | `string / object` |
| `standardDuration` | `Integer` | `number` |
| `keyStep` | `Boolean` | `boolean` |
| `minStaySeconds` | `Integer` | `number` |
| `confirmRequired` | `Boolean` | `boolean` |
| `parameterRequired` | `Boolean` | `boolean` |
| `photoRequired` | `Boolean` | `boolean` |
| `skipAllowed` | `Boolean` | `boolean` |
| `abnormalHandling` | `String` | `string / object` |
| `qualityItemId` | `Long` | `number` |
| `andonTypeId` | `Long` | `number` |
| `enabled` | `Boolean` | `boolean` |
| `createdAt` | `LocalDateTime` | `string (ISO-8601)` |
| `updatedAt` | `LocalDateTime` | `string (ISO-8601)` |

### `SopStepExecutionRecord`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopStepExecutionRecord.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `executionId` | `Long` | `number` |
| `snapshotId` | `Long` | `number` |
| `taskId` | `Long` | `number` |
| `stepId` | `Long` | `number` |
| `stepNo` | `Integer` | `number` |
| `viewStartedAt` | `LocalDateTime` | `string (ISO-8601)` |
| `confirmedAt` | `LocalDateTime` | `string (ISO-8601)` |
| `staySeconds` | `Integer` | `number` |
| `parameterJson` | `String` | `string / object` |
| `photoAttachmentId` | `Long` | `number` |
| `result` | `String` | `string / object` |
| `operatorId` | `Long` | `number` |
| `idempotencyKey` | `String` | `string / object` |

### `SopTaskContext`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopTaskContext.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `taskId` | `Long` | `number` |
| `taskNo` | `String` | `string / object` |
| `workOrderId` | `Long` | `number` |
| `productId` | `Long` | `number` |
| `routeId` | `Long` | `number` |
| `lineId` | `Long` | `number` |
| `workstationId` | `Long` | `number` |
| `status` | `String` | `string / object` |

### `SopTaskPackage`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopTaskPackage.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `document` | `SopDocument` | `string / object` |
| `version` | `SopVersion` | `string / object` |
| `snapshot` | `SopTaskSnapshot` | `string / object` |
| `execution` | `SopExecutionRecord` | `string / object` |
| `steps` | `List<SopStep>` | `array` |
| `attachments` | `List<SopAttachment>` | `array` |
| `stepRecords` | `List<SopStepExecutionRecord>` | `array` |

### `SopTaskSnapshot`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopTaskSnapshot.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `taskId` | `Long` | `number` |
| `sopId` | `Long` | `number` |
| `versionId` | `Long` | `number` |
| `versionNo` | `String` | `string / object` |
| `modelId` | `Long` | `number` |
| `modelVersionId` | `Long` | `number` |
| `modelSha256` | `String` | `string / object` |
| `snapshotJson` | `String` | `string / object` |
| `lockedBy` | `Long` | `number` |
| `lockedAt` | `LocalDateTime` | `string (ISO-8601)` |
| `matchRule` | `String` | `string / object` |
| `status` | `String` | `string / object` |

### `SopVersion`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/sop/SopVersion.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `sopId` | `Long` | `number` |
| `versionNo` | `String` | `string / object` |
| `revisionType` | `String` | `string / object` |
| `status` | `String` | `string / object` |
| `effectiveFrom` | `LocalDateTime` | `string (ISO-8601)` |
| `effectiveTo` | `LocalDateTime` | `string (ISO-8601)` |
| `submitBy` | `Long` | `number` |
| `submitAt` | `LocalDateTime` | `string (ISO-8601)` |
| `reviewBy` | `Long` | `number` |
| `reviewAt` | `LocalDateTime` | `string (ISO-8601)` |
| `approveBy` | `Long` | `number` |
| `approveAt` | `LocalDateTime` | `string (ISO-8601)` |
| `publishBy` | `Long` | `number` |
| `publishAt` | `LocalDateTime` | `string (ISO-8601)` |
| `modelVersionId` | `Long` | `number` |
| `remark` | `String` | `string / object` |
| `createdAt` | `LocalDateTime` | `string (ISO-8601)` |
| `updatedAt` | `LocalDateTime` | `string (ISO-8601)` |

### `SysMenu`

来源：`backend/mes-system/src/main/java/com/fanmes/system/domain/entity/SysMenu.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `parentId` | `Long` | `number` |
| `menuCode` | `String` | `string / object` |
| `menuName` | `String` | `string / object` |
| `moduleCode` | `String` | `string / object` |
| `path` | `String` | `string / object` |
| `sortNo` | `Integer` | `number` |

### `SysUser`

来源：`backend/mes-auth/src/main/java/com/fanmes/auth/domain/entity/SysUser.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `username` | `String` | `string / object` |
| `password` | `String` | `string / object` |
| `displayName` | `String` | `string / object` |
| `mobile` | `String` | `string / object` |
| `employeeNo` | `String` | `string / object` |
| `orgId` | `Long` | `number` |
| `teamId` | `Long` | `number` |
| `status` | `String` | `string / object` |

### `TableSpec`

来源：`backend/mes-equipment/src/main/java/com/fanmes/equipment/domain/TableSpec.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `resource` | `String` | `string / object` |
| `table` | `String` | `string / object` |
| `writableColumns` | `List<String>` | `array` |
| `requiredColumns` | `List<String>` | `array` |
| `filterColumns` | `List<String>` | `array` |
| `statusColumn` | `String` | `string / object` |
| `defaultStatus` | `String` | `string / object` |
| `fromClause` | `String` | `string / object` |
| `columnPrefix` | `String` | `string / object` |
| `selectExpression` | `String` | `string / object` |

### `TableSpec`

来源：`backend/mes-integration/src/main/java/com/fanmes/integration/domain/TableSpec.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `resource` | `String` | `string / object` |
| `table` | `String` | `string / object` |
| `writableColumns` | `List<String>` | `array` |
| `requiredColumns` | `List<String>` | `array` |
| `filterColumns` | `List<String>` | `array` |
| `statusColumn` | `String` | `string / object` |
| `defaultStatus` | `String` | `string / object` |

### `UserVO`

来源：`backend/mes-auth/src/main/java/com/fanmes/auth/domain/vo/UserVO.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `displayName` | `String` | `string / object` |

### `WorkOrder`

来源：`backend/mes-production/src/main/java/com/fanmes/production/domain/WorkOrder.java`

| 字段 | Java 类型 | 前端建议类型 |
|---|---|---|
| `id` | `Long` | `number` |
| `workOrderNo` | `String` | `string / object` |
| `externalNo` | `String` | `string / object` |
| `sourceType` | `String` | `string / object` |
| `productId` | `Long` | `number` |
| `planQty` | `BigDecimal` | `number` |
| `completedQty` | `BigDecimal` | `number` |
| `qualifiedQty` | `BigDecimal` | `number` |
| `defectiveQty` | `BigDecimal` | `number` |
| `unitId` | `Long` | `number` |
| `plannedStartAt` | `LocalDateTime` | `string (ISO-8601)` |
| `plannedEndAt` | `LocalDateTime` | `string (ISO-8601)` |
| `deliveryDate` | `LocalDate` | `string (ISO-8601)` |
| `lineId` | `Long` | `number` |
| `routeId` | `Long` | `number` |
| `status` | `String` | `string / object` |

---

源 Controller：
- `backend/mes-auth/src/main/java/com/fanmes/auth/controller/AuthController.java`
- `backend/mes-equipment/src/main/java/com/fanmes/equipment/api/EquipmentRecordController.java`
- `backend/mes-gateway/src/main/java/com/fanmes/gateway/api/GatewayController.java`
- `backend/mes-integration/src/main/java/com/fanmes/integration/api/IntegrationController.java`
- `backend/mes-production/src/main/java/com/fanmes/production/controller/AndonController.java`
- `backend/mes-production/src/main/java/com/fanmes/production/controller/BarcodeController.java`
- `backend/mes-production/src/main/java/com/fanmes/production/controller/ProductionController.java`
- `backend/mes-production/src/main/java/com/fanmes/production/controller/ProductionStatusController.java`
- `backend/mes-production/src/main/java/com/fanmes/production/controller/SopController.java`
- `backend/mes-quality/src/main/java/com/fanmes/quality/controller/QualityController.java`
- `backend/mes-quality/src/main/java/com/fanmes/quality/controller/QualityStatusController.java`
- `backend/mes-report/src/main/java/com/fanmes/report/controller/ReportController.java`
- `backend/mes-system/src/main/java/com/fanmes/system/controller/SystemController.java`
